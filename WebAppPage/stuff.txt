   
   //Top alert Codes : 

---HTML
    <div id="alert-banner" class="alert-banner">
    <div class="alert-content">
        <i class="fi fi-rr-exclamation"></i> <span id="alert-text">لازم تعرف: استخدامك لخدمات مدخل يعني موافقتك على سياسات الاستخدام</span>
    </div>
    <button class="close-alert" onclick="closeAlert()">✕</button>
</div>
--- JAVA
document.addEventListener("DOMContentLoaded", () => {
    // اسم مفتاح الجلسة (لو تبي التنبيه يطلع للكل مرة ثانية حتى لو قفلوه، غير الاسم هذا مثلاً alert_v2)
    const SESSION_KEY = 'madkhal_alert_v1';
    
    const banner = document.getElementById('alert-banner');
    
    // إذا الشريط موجود في الـ HTML وما قد تقفل في هذي الجلسة
    if (banner && !sessionStorage.getItem(SESSION_KEY)) {
        setTimeout(() => {
            banner.classList.add('show'); // أظهره بأنميشن
            document.body.classList.add('has-alert'); // نزل الصفحة شوي
        }, 500); // تأخير بسيط نص ثانية عشان يطلع بشكل جميل
    }
});

function closeAlert() {
    const banner = document.getElementById('alert-banner');
    const SESSION_KEY = 'madkhal_alert_v1';

    if (banner) {
        banner.classList.remove('show'); // اخفاء
        document.body.classList.remove('has-alert'); // ترجيع الصفحة مكانها
        
        // حفظ في الجلسة أن المستخدم قفله (عشان ما يرجع يزعجه لين يقفل المتصفح)
        sessionStorage.setItem(SESSION_KEY, 'true');
    }
}
--- CSS
/* تنسيق شريط التنبيه */
.alert-banner {
    background-color: #ffcdcd; /* لون خلفية أصفر فاتح */
    color: #850415; /* لون النص */
    border-bottom: 1px solid #ffbaba;
    padding: 12px 20px;
    position: fixed; /* عشان يثبت فوق */
    top: 0;
    left: 0;
    right: 0;
    z-index: 9999; /* فوق كل شي */
    display: flex;
    justify-content: space-between;
    align-items: center;
    transform: translateY(-100%); /* مخفي افتراضياً (يطلع من فوق) */
    transition: transform 0.3s ease-in-out;
    box-shadow: 0 2px 5px rgba(0,0,0,0.1);
}

.alert-banner.show {
    transform: translateY(0); /* إظهار الشريط */
}

.alert-content {
    display: flex;
    align-items: center;
    gap: 10px;
    font-size: 14px;
    font-weight: bold;
}

.close-alert {
    background: none;
    border: none;
    font-size: 18px;
    cursor: pointer;
    color: #850404;
    padding: 0 5px;
}

/* عشان المحتوى ما يختفي تحت الشريط */
body.has-alert {
    margin-top: 50px; /* ندف الموقع تحت عشان التنبيه ياخذ راحته */
}


-------------------------------------

//Alert popup:

---HTML
<div id="custom-popup" class="popup-overlay">
    <div class="popup-box">
        <div class="popup-icon">
            <i class="fi fi-rr-bell-ring"></i> </div>
        <h3 class="popup-title">تنويه هام</h3>
        <p class="popup-text">
            تم تحديث جدول الاختبارات النهائية. يرجى الاطلاع على الجدول الجديد لتجنب التعارض.
        </p>
        <div class="popup-buttons">
            <a href="https://example.com" class="btn-primary" onclick="closePopup()">عرض الجدول</a>
            
            <button class="btn-secondary" onclick="closePopup()">لاحقاً</button>
        </div>
    </div>
</div>

---CSS
/* خلفية الشاشة (الظلام) */
.popup-overlay {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(0, 0, 0, 0.5); /* لون أسود شفاف */
    backdrop-filter: blur(4px); /* تغبيش الخلفية يعطي فخامة */
    display: flex;
    justify-content: center;
    align-items: center;
    z-index: 9999999999;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
}

/* ظهور النافذة */
.popup-overlay.active {
    opacity: 1 !important;
    visibility: visible !important;
    display: flex !important;
}

/* صندوق التنبيه نفسه */
.popup-box {
    background: white;
    padding: 30px;
    border-radius: 30px;
    width: 90%;
    max-width: 400px;
    text-align: center;
    transform: scale(0.8);
    transition: transform 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275); /* أنميشن مطاطي حلو */
    box-shadow: 0 15px 30px rgba(0,0,0,0.2);
}

.popup-overlay.active .popup-box {
    transform: scale(1);
}

/* الأيقونة */
.popup-icon {
    width: 60px;
    height: 60px;
    background: #dee5fa; /* خلفية زرقاء فاتحة */
    color: #1309cf; /* لون الأيقونة */
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 24px;
    margin: 0 auto 15px;
}

/* النصوص */
.popup-title {
    margin: 0 0 10px;
    font-size: 1.2rem;
    color: #333;
}

.popup-text {
    font-size: 0.95rem;
    color: #666;
    margin-bottom: 25px;
    line-height: 1.5;
}

/* الأزرار */
.popup-buttons {
    display: flex;
    gap: 10px;
    justify-content: center;
}

.popup-buttons .btn-primary {
    background: linear-gradient(90deg, #103191, #106091);
    color: white;
    border: none;
    padding: 10px 20px;
    border-radius: 11px;
    text-decoration: none;
    font-weight: bold;
    flex: 1;
    cursor: pointer;
}

.popup-buttons .btn-secondary {
    background: #f3f4f6;
    color: #4b5563;
    font-family: 'tajawal';
    border: none;
    padding: 10px 20px;
    border-radius: 11px;
    font-weight: bold;
    cursor: pointer;
    flex: 1;
}

.popup-buttons .btn-secondary:hover {
    background: #e5e7eb;
}




---JAVA
document.addEventListener("DOMContentLoaded", () => {
    // نحدد اسم مفتاح الجلسة (عشان المتصفح يتذكر)
    const POPUP_KEY = 'portal_popup_v1';
    
    // إذا ما قد شافه في هذي الجلسة
    if (!sessionStorage.getItem(POPUP_KEY)) {
        setTimeout(() => {
            document.getElementById('custom-popup').classList.add('active');
        }, 1000); // يطلع بعد ثانية وحدة من دخول الموقع
    }
});

function closePopup() {
    const popup = document.getElementById('custom-popup');
    const POPUP_KEY = 'portal_popup_v1';
    
    // إخفاء النافذة
    popup.classList.remove('active');
    
    // حفظ في الذاكرة أنه شافه خلاص
    sessionStorage.setItem(POPUP_KEY, 'true');
}